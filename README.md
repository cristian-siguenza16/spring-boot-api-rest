## First need to run de yml with the credentials

## The Keycloak will run on the port 9090, the credentials will be admin, admin
## Then need to create a realm with the name "spring-boot-dev"
## Then need to create a client with the name "spring-boot-api-rest"
## Then need to create an admin realm role, with a client-role-admin

## After creating this initialize the project and test it.

